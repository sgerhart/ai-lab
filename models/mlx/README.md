# MLX

Apple Silicon native experiments on the Studio. M5 Max compatibility **unverified**.

```bash
cd models/mlx
uv sync
```

Do not run `uv sync` as part of host bootstrap. This project has no MLX dependency pinned until the first Studio preflight confirms import.
