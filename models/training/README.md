# Training / fine-tune / LoRA

Studio only. Checkpoints stay under `AI_LAB_MODEL_ROOT` (not in Git).

No experiment is authorized. The entrypoint refuses:

```bash
./scripts/train.sh
```

Do not download datasets from this repository.
