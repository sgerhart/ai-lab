# Operator workstation snapshot (2026-09-19)

Canonical host: [`hosts/m3-air/`](../../hosts/m3-air/README.md).

Observed on the machine that holds this workspace: Apple M3, Darwin 25.6.0, `sysctl hw.memsize` = 16 GB. Owner has not attested RAM. Ollama was listening on `*:11434` with no models (F-003).
