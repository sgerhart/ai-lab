# Operator workstation snapshot (2026-09-19)

Canonical host: [`hosts/m3-air/`](../../hosts/m3-air/README.md). Tailscale machine name: `mac-air` (ADR 0032).

Observed on the machine that holds this workspace: Apple M3, Darwin 25.6.0, `sysctl hw.memsize` = 16 GB. Owner attested 16 GB on 2026-09-21 (ADR 0031). Ollama was listening on `*:11434` with no models (F-003, accepted ADR 0029).
