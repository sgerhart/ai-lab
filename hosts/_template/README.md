# Host template

Copy to `hosts/<short-name>/` with README.md, Brewfile, and a setup.sh wrapper around `scripts/host-setup.sh` (extend that script with a new `--host` role — do not fork logic).

Overlay: copy `local.inventory.yaml.example` to `local.inventory.yaml` (gitignored).
