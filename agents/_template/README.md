# Agent template

Copy to `agents/<id>/README.md` when registering a lab agent.

Required headings:

- Purpose
- Owner
- Runtime (Cursor, compose service, cron, other)
- Model backend (catalog id)
- Tools allowed
- Data scope
- Network scope
- Secrets required (names only)
- How to stop it
