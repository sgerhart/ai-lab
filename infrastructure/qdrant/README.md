# Qdrant

Vector memory. Metadata keys should store Postgres work-order ids.

API key is required via `QDRANT_API_KEY` in compose. The example env value is not a live secret.

Image pin: `qdrant/qdrant:v1.13.4` — not verified on the M1 yet.
