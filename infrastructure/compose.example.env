# Interpolation-only example. Not production credentials.
# Copy to a gitignored file (for example infrastructure/compose.local.env) before a real up.
# CI uses this file with docker compose config.

AI_LAB_BIND_ADDRESS=127.0.0.1
POSTGRES_USER=ai_lab
POSTGRES_PASSWORD=change-me-not-for-live
POSTGRES_DB=ai_lab
POSTGRES_PORT=5432
REDIS_PORT=6379
REDIS_PASSWORD=change-me-not-for-live
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_API_KEY=change-me-not-for-live
